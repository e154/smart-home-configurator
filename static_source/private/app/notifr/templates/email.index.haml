.row
  .col-md-12
    %h3 {{ 'notifr.email_system' | translate }}

.row
  .col-md-12
    .form-group
      %a(ui-sref="dashboard.notifr.new_template" if-can="{notifr:['create_notifr_template']}")
        .fa.fa-plus
        &nbsp;
        {{'notifr.add_template' | translate}}
      ,
      &nbsp;
      %a(ui-sref="dashboard.notifr.items" if-can="{notifr:['read_notifr_item']}")
        &nbsp;
        {{'notifr.element_of_template' | translate}}

.row(if-can="{notifr:['read_notifr_template']}")
  .col-md-12
    .table(table="ctrl.options")